apiVersion: "agones.dev/v1"
kind: GameServer
metadata:
  generateName: "game-server-"
spec:
  ports:
  - name: default
    portPolicy: Dynamic
    containerPort: 7777
  template:
    spec:
      containers:
      - name: game-server
        image: https://raw.githubusercontent.com/JausGames/AgonesServerTest/main/gameserver.yaml
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "512Mi"
            cpu: "500m"
